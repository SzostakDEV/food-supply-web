import { Injectable } from '@angular/core';
import { HttpBaseService } from '../shared/http.base.service';

@Injectable()
export class RegisterService extends HttpBaseService {
  register(login, password): Promise<any> {
    const body
  }
}
